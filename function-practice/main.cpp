#include <iostream>
#include "basic.h"
#include "iteration.h"

using namespace std;

int main ()
{
    /*
    // Calculat area of rectangle
    cout << "Area of 4x4 rectangle: " << rectangle_area(4, 4) << endl;

    // extract the fraction
    cout << "Fraction of float 12.3334: " << fraction_float(12.3334) << endl;

    // draw a rectangle
    draw_rectange(4, 4);

    // prime number test case
    for (unsigned int count = 2; count < 100; count ++)
    {
        if (prime_number (count))
             cout << count << " is prime number:"  << endl;
    }
    */
    // reverse a number
    cout << "reverse 12345 : " << number_reverse (12345) << endl;
}
